package com.trucks.domain.validation;

import java.util.ArrayList;
import java.util.List;

public class PortValidator {
    
    /*public List<ApiSubError> validate(Port port) {
        List<ApiSubError> errors = new ArrayList<>();
        errors.addAll(this.validateName(trap.getName()));
        errors.addAll(this.validateDescription(trap.getDescription()));
        errors.addAll(this.validateIsland(trap.getIsland()));
        errors.addAll(this.validateType(trap.getType()));
        errors.addAll(this.validateEntryPoint(trap.getEntryPoint()));
        errors.addAll(this.validateGeolocation(trap.getGeolocation()));
        errors.addAll(this.validateImagesId(trap.getImages()));
        errors.addAll(this.validateInstallationDate(trap.getInstallationDate()));
        errors.addAll(this.validateLeavingDate(trap.getLeavingDate()));
        return errors;
    }*/
}
